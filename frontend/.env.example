# 前端构建时的环境变量
# 本地开发时不用填这个文件（走 Vite 代理）。
# 部署到 Netlify 时，请到 Netlify 控制台 → Site settings → Build & deploy → Environment 设置以下两项：

# 后端公网地址（部署到 Render 后会拿到，类似 https://zuoyepigai-backend.onrender.com）
VITE_API_BASE_URL=

# 访问令牌：必须与后端 ACCESS_TOKEN 完全一致，用来防止陌生人直接调用接口
VITE_ACCESS_TOKEN=
